{
    "version": 2,
    "builds": [
      {
        "src": "server.ts",
        "use": "@vercel/node"
      }
    ],
    "routes": [
      { "src": "/(.*)", "dest": "dist/server.js" }  
    ],
    "installCommand": "npm install",
    "buildCommand": "npm run build"
  }
  